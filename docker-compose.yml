services:
  hermes:
    build: .
    restart: unless-stopped
    volumes:
      - hermes_data:/opt/data
    environment:
      - HERMES_UID=10000
      - HERMES_GID=10000
    command: ["gateway", "run"]

volumes:
  hermes_data:
    driver: local
